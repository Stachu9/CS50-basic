#include <cs50.h>
#include <math.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{
    if (argc < 2 || argc > 2)
    {
        printf("Usage: %i\n", argv[0])
    }
    do
    {
        string plainText = get_string(plainText);
    }
    while(!plainText);

}