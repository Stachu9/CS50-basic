#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long cardNum;
        do
        {
            cardNum = get_long("Enter card number: ");
        }
        while (!cardNum);

    int count = 0;
        do{
            numberDigs = numberDigs / 10;
            ++count;
        }while (numberDigs != 0);

    int sumOnce = 0;
    int sumTwice = 0;
    int flag = 1;

        for(int i = 0; i < numberDigs; i ++)
        {
            
        }
}