#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long cardNum;
        do
        {
            cardNum = get_long("Enter card number: ");
        }
        while (!cardNum);

    int count = 0;
        do{
            numberDigs = numberDigs / 10;
            ++count;
        }while (numberDigs != 0);

        for(int i = 0; i < numberDigs; i += 2)
        {
            
        }
}